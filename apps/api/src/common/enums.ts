export enum Role {
  ADMIN = 'admin',
  CREATOR = 'creator',
  USER = 'user',
}

export enum UploadedFileType {
  IMAGE = 'IMAGE',
  VIDEO = 'VIDEO',
  DOCUMENT = 'DOCUMENT',
}

export enum CurrencyCode {
  USD = 'usd',
}

export enum CurrencySymbol {
  USD = '$',
}
