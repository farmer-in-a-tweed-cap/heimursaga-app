import { VerifiedIcon } from 'lucide-react';

export const VerificationBadge = () => (
  <div className="w-[14px] h-[14px] rounded-full bg-primary text-white font-medium flex items-center justify-center">
    <VerifiedIcon size={14} />
  </div>
);
